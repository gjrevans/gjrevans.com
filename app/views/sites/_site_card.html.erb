<% @sites.each do |site| %>
<div class="card">
    <%= image_tag site.image, class:"card-img-top", alt:"codego" %>
    <div class="card-block">
        <h4 class="card-title"><%= site.title %></h4>
        <p class="card-text"><%= site.description %></p>
        <div class="row">
            <div class="col-xs-6">
                <%= link_to site_path(site), class: "btn btn-block btn-outline-primary", target: "blank" do %>
                <i class="fa fa-fw fa-rocket mr-1"></i>Launch
                <% end %>
            </div>
            <div class="col-xs-6">
                <%= link_to site.github, class: "btn btn-block btn-outline-primary", target: "blank" do %>
                <i class="fa fa-fw fa-github mr-1"></i>Github
                <% end %>
            </div>
        </div>
        <% if user_signed_in? %>
            <%= link_to 'Edit', edit_site_path(site), class: "mt-3" %>
        <% end %>
    </div>
</div>
<% end %>
